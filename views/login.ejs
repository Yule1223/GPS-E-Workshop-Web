<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>Login</title>
    <link rel="stylesheet" href="/css/login.css">
</head>

<body>

<div class="barraNavegacion">
    <div class="icono"><img src="/img/LogoGPS.png"></div>
</div>

<div class="cuerpo-principal">

    <form class="formulario" action="/empleados/login" method="POST">

        <label class="etiquetas-formulario">Email:</label>
        <input type="email" name="email">

        <label class="etiquetas-formulario" style="margin-top: 20px">Contraseña:</label>
        <input type="password" name="password">

        <% if (errorMsg) { %>
        <p style="color:red;text-align: center;font-size: 18px;padding-top: 4px;">
            <%=errorMsg%>
        </p>

        <% } else { %>
        <p></p>
        <% } %>

        <div class="formularioboton">
            <button class="btn btn-principal" type="submit" style="margin-top: 20px">Login</button>
        </div>

    </form>

</div>

</body>
</html>